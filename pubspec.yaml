name: easy_setup
description: A starting point for Dart libraries or applications.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  args: ^2.7.0 # 터미널 인자(--flavor dev 등)를 파싱할 때
  yaml: ^3.1.3 # 사용자의 pubspec.yaml 설정을 읽어올 때
  path: ^1.9.0
  image: ^4.5.0 # 앱 아이콘 리사이즈

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  easy_setup: easy_setup  # bin/easy_setup.dart를 'easy_setup' 명령어로 실행
